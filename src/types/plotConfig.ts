// Kept for backward compatibility. Canonical config types live in src/config/defaultPlotConfig.ts.
export type {
  AxisConfig,
  AnnotationConfig,
  ColoredAreaConfig,
  DataframeConfig,
  FrameConfig,
  GuidelineConfig,
  LayerConfig,
  PlotConfig,
} from '../config/defaultPlotConfig'
